appVersion: v1
kind: Service
metadata:
  name: {{ include "gem-app.fullname" . }}
  labels:
    {{- include "gem-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "gem-app.selectorLabels" . | nindent 4 }}